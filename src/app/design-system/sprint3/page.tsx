import { Sprint3Preview } from "@/components/community/sprint3-preview";

export const metadata = {
  title: "Sprint 3 preview",
  description: "UI preview for reputation dashboard and mock community feed."
};

export default function Sprint3PreviewPage() {
  return <Sprint3Preview />;
}

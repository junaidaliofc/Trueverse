import { Sprint7Preview } from "@/components/launch/sprint7-preview";

export const metadata = {
  title: "Sprint 7 — Notifications, search, launch",
  description: "Global search, notifications, profile completion, onboarding, and discovery."
};

export default function Sprint7PreviewPage() {
  return <Sprint7Preview />;
}

import { Sprint8Preview } from "@/components/launch/sprint8-preview";

export const metadata = {
  title: "Sprint 8 — Trust OS",
  description: "Admin moderation, appeals, badges, profile health, and launch readiness."
};

export default function Sprint8PreviewPage() {
  return <Sprint8Preview />;
}
